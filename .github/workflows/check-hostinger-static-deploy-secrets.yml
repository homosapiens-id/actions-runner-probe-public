name: check-hostinger-static-deploy-secrets
on: push
jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - name: Check secret presence
        env:
          FTP_PASS: ${{ secrets.HOSTINGER_FTP_PASSWORD }}
          FTP_USER: ${{ secrets.HOSTINGER_FTP_USER }}
          FTP_HOST: ${{ secrets.HOSTINGER_FTP_HOST }}
          SFTP_KEY: ${{ secrets.HOSTINGER_SFTP_KEY }}
          SSH_KEY: ${{ secrets.HOSTINGER_SSH_KEY }}
          SSH_HOST: ${{ secrets.HOSTINGER_SSH_HOST }}
          SSH_USER: ${{ secrets.HOSTINGER_SSH_USER }}
        run: |
          set -eu
          ftp=0
          if [ -n "${FTP_PASS:-}" ] && [ -n "${FTP_USER:-}" ] && [ -n "${FTP_HOST:-}" ]; then ftp=1; fi
          ssh=0
          if [ -n "${SSH_KEY:-${SFTP_KEY:-}}" ] && [ -n "${SSH_USER:-}" ] && [ -n "${SSH_HOST:-}" ]; then ssh=1; fi
          echo "ftp_ready=$ftp"
          echo "ssh_ready=$ssh"
          if [ "$ftp" = "0" ] && [ "$ssh" = "0" ]; then
            echo "NO_STATIC_DEPLOY_CREDENTIALS"
            exit 42
          fi
